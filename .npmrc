; Use legacy peer-dep resolution to work around an npm 10.9 dedup
; crash ("Invalid Version:") triggered by transitive peer constraints
; in the firebase-admin → google-auth-library → teeny-request chain.
; This setting applies to local dev AND Vercel installs.
legacy-peer-deps=true
